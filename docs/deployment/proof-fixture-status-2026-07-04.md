# Vennek proof fixture status — 2026-07-04

## Requested target

Create a real Blockfrost proof fixture so `BLOCKFROST_TEST_TX_HASH` + `BLOCKFROST_TEST_CONTENT_HASH` can make `npm run staging:smoke` verify the on-chain `vennek.proof.v1` metadata payload.

## Prepared fixture payload

Generated by:

```bash
VENNEK_PROOF_FIXTURE_NOW=2026-07-04T00:00:00.000Z npm run proof:fixture
```

Output file:

```text
samples/proof-fixtures/vennek-proof-fixture.json
```

Content hash:

```text
sha256:6d40b866557eda90c615a83ec3b9699dd71a3d7bbabfd9c0d2de83a143977ad2
```

Metadata payload to submit externally in a Cardano transaction:

```json
{
  "schema": "vennek.proof.v1",
  "content_hash": "sha256:6d40b866557eda90c615a83ec3b9699dd71a3d7bbabfd9c0d2de83a143977ad2",
  "source_refs": [
    "https://t.me/cardano_claw_bot"
  ],
  "created_at": "2026-07-04T00:00:00.000Z",
  "agent_version": "0.1.0",
  "report_id": "vennek-staging-proof-fixture"
}
```

## Verification status

Local gates passed:

```text
npm run typecheck: pass
npm run build: pass
npm run staging:smoke: pass for VENNEK_DATA_DIR, telegram.getMe, blockfrost.latestBlock
```

On-chain proof fixture is still not complete:

```text
SKIP blockfrost.proofFixture - BLOCKFROST_PROJECT_ID, BLOCKFROST_TEST_TX_HASH, and BLOCKFROST_TEST_CONTENT_HASH are required for proof fixture verification.
```

## Blocker

Creating the final on-chain fixture requires a funded Cardano wallet and signing/submitting a transaction with metadata. Current local checks found:

- no usable `cardano-cli` command in PATH;
- no safely usable local Cardano signing key found;
- the candidate `cardano-wallet.txt` is source code, not a usable wallet key;
- the current Blockfrost project is mainnet, so a successful fixture would likely require a mainnet transaction fee unless a separate preprod/preview project key and testnet funds are provided.

Because this is a real blockchain transaction with signing and fees, the agent did not fabricate a tx hash or submit with unknown funds.

## To finish

Provide one of:

1. a real transaction hash containing the metadata above and set:
   - `BLOCKFROST_TEST_TX_HASH=<tx_hash>`
   - `BLOCKFROST_TEST_CONTENT_HASH=sha256:6d40b866557eda90c615a83ec3b9699dd71a3d7bbabfd9c0d2de83a143977ad2`

2. a funded preprod/preview setup:
   - Blockfrost preprod/preview project id;
   - funded testnet signing key/address or a manual wallet-signing path.

3. explicit approval to use a funded mainnet wallet/address and pay the transaction fee for submitting this metadata.
